//
   Created by Administrator on 15-1-23.
//
    相对布局瀑布流


doctype html
html
    head
        - if(publisher)
            title #{publisher.nickname}直播间：#{room}
        - else
            title 直播号:#{room}
        meta(name="viewport",content="width=device-width,initial-scale=1.0,user-scalable=no")
        link(rel='stylesheet', href='/stylesheets/style_new.css')
        link(rel='stylesheet', href='/stylesheets/bootstrap.css')
        link(rel='stylesheet', href='//netdna.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css')
        style.
            .back_to_add{
                position:fixed;
                right:0;
                top:0;
            }
    body
        //div#tools-btn
            div
            div
            div
        //div#tools-panel
            - if (publisher)
                div.myLiveRoom 我的直播间
                div.addProduct
                    a(href='/publish.html') 发布
                div.myFavorite 我的收藏
            -else
                div.changeRoom
                    a(href="/we_account/room_door")切换房间
                div.favorite 收藏一下
                div.myFavorite 我的收藏
        //- if (publisher)
            div.user 当前用户:
                span #{publisher.user_name}
        //- if (products.length)
        section.waterfall
            div.column
            div.column
            div.column
            div.column
            div.column
            div.column
            div.column
            div.column
                //each product in products
                //    div.box.unvisible
                //        div(data-desc='#{product.text}').desc #{product.text}
                //        div(data-imgnum='#{product.image_url.length}').img-display
                //            each imageName,i in product.image_url
                //                img(src='http://120.24.224.144/images/#{imageName}' data-num='#{i}')
                //        div.ontact-saler
                //            联系卖家
        div(role="dialog" aria-hidden="true" data-backdrop="static")#popup.modal.fade
            div(data-toggle="modal" data-target="#popup")#popup-close
                span ×
            div.modal-dialog
                div.modal-content
                    div.modal-header header
                        button(type="button" class="close" data-dismiss="modal")
                            span(aria-hidden="true") x
                            span.sr-only close
                    div.modal-body
                        div.big-img-display
                        //div#next next
        //button(data-toggle="modal" data-target="#popup")#popup-btn popup-btn
        div#tools-box
            - if(publisher)
                div#upload-div
                    button#upload
                        div.vertical
                        div.horizon
                div.user 当前用户:
                    span #{publisher.user_name}
            - else
                div#tools-btn
                    div
                    div
                    div
                div#tools-panel
                    div.changeRoom
                        a(href="/we_account/room_door") 切换房间
                    div.myFavorite 我的收藏
                div.favorite
                    - if (isFavorite)
                        i.fa.fa-heart
                    - else
                        i.fa.fa-heart-o

        div#upload-panel
            div(role="alert")#warn.alert.alert-warning.fade.in 至少需要发布一张图片
            div#sub-btn
                input(type="button" value="确定")#submit
            textarea(placeholder="给点介绍吧...").product-desc
            div#image_content
                div#upload-div2
                    button(data-type="pop-btn")#upload2
                        div.vertical
                        div.horizon
            div#cancel-btn
                input(type="button" value="取消")#cancel

        div#uploading-mask

        //div(data-dismiss="alert")#warn.fade.in

        //button(value="" style="position:fixed;left:0;top:0;")#alertTest 测试alert

script(src="/javascripts/jquery.js")
script(src="/javascripts/libs/jquery.mobile.custom.js")
script(src="/javascripts/libs/bootstrap.min.js")
script(src="/javascripts/preloadImg.js")
script(src="/javascripts/waterfall_relative.js")
script(src="/javascripts/ajaxupload.js")
script(src="/javascripts/live_room.js")
script.
    function load(){
        waterfall.asyncLoader();
    }